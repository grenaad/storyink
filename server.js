// OpenCode plugin entrypoint. OpenCode resolves `<package>/server` (then `<package>`)
// for npm packages and `<dir>/server` (then `<dir>/index`) for local directories.
export { default } from "./dist/index.js"
